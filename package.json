{
  "name": "execution-runtime-lab",
  "version": "1.0.0",
  "description": "Runtime implementation experimentation for Execution Authority Runtime",
  "type": "module",
  "scripts": {
    "demo:openclaw": "npx ts-node demo/openclaw_intercept_demo.ts",
    "proof:generate": "tsx proof/generate_proof_artifact.ts",
    "test": "jest",
    "build:runtime": "tsc",
    "build:stop": "tsc --project tsconfig.stop.json && rm -rf dist/stop/src/executor dist/stop/src/adapter/allow_handler.* && echo '✓ Executor modules removed from STOP build'",
    "verify:binary-absence": "npm run build:stop && ! grep -r 'executeAction\\|createExecutionFunction\\|allow_execution' dist/stop"
  },
  "keywords": [
    "execution-authority-runtime",
    "openclaw",
    "runtime-mediation",
    "decision-logging"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
